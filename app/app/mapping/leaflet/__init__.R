#' @export
box::use(
  app / mapping / leaflet / ui[...],
  app / mapping / leaflet / make_map[...],
  app / mapping / leaflet / update_map_content[...],
  app / mapping / leaflet / prediction_markers[...],
  app / mapping / leaflet / update_tour_map[...],
)
